// This is an independent project of an individual developer. Dear PVS-Studio, please check it.
// PVS-Studio Static Code Analyzer for C, C++, C#, and Java: https://pvs-studio.com

#include "visuals.hpp"

#include <tinyformat/tinyformat.h>

void draw_fov(float fov, color_t color) {
	//if (config::context.legit.show_fov_filled)
	//	g_render.circle_filled(g_cs2->screen.x / 2, g_cs2->screen.y / 2, fov * 10, color);
	//g_render.circle(g_cs2->screen.x / 2, g_cs2->screen.y / 2, fov * 10, color);
}

void c_visuals::run_overlay_render() {
	//draw_fov(config::context.legit.fov, color_t(255, 0, 0, 20));
}